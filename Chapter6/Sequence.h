#ifndef SEQUENCE_H
#define SEQUENCE_H


namespace Sequence {
	enum StateId {
		STATE_TITLE,
		STATE_STAGE_SELECT,
		STATE_GAME_LOAD,
		STATE_GAME,
		STATE_MENU,
		STATE_CLEAR,
		STATE_END,
	};
};


#endif // MOVE_H
